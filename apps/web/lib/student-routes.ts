/** The `(student)` paths named from more than one place. */
export const STUDENT_ROUTES = {
  /** "Who's learning today?" — the child's front door (FR-AUTH-06). */
  selectProfile: "/select-profile",
  home: "/home",
} as const;
