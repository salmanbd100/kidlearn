# Copy to .env.local. Everything here is NEXT_PUBLIC_ and therefore shipped to
# the browser — never put a secret in this file.

# Base URL of apps/server. Defaults to http://localhost:4000 when unset.
NEXT_PUBLIC_API_URL=http://localhost:4000
